# Security policy

This repository is a benchmarking harness, not a production cryptographic
library. Benchmark results are not a security audit or an endorsement of any
scheme.

Report vulnerabilities privately through GitHub's security advisory feature.
Do not include secrets or embargoed implementation details in public issues.

The benchmark inputs are public and deterministic. Never add production keys,
private fixtures, credentials, or proprietary traces to result artifacts.

